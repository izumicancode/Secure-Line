# Secure-Line
A verified, end-to-end-encrypted, mesh-relayed chat app for your LAN , a private, encrypted chat mesh. Every device on the network discovers the others automatically, every conversation is end-to-end encrypted with a ratcheting key schedule (the same idea Signal uses), and nothing ever touches a server, because there isn't one.
